---
title: JDK1.8对HashMap除了红黑树还有哪些改动？
date: 2026-00-00
categories:
  - 八股
tags:
  - Java
  - Java集合
---

## JDK1.8对HashMap除了红黑树还有哪些改动?

**哈希函数简化、扩容机制优化、头插法改尾插法**

1. 哈希函数简化：1.7 的 hash 函数经过四次异或运算，1.8 只做一次，把 key 的 hashCode 高16位和低16位异或一下就完事了。这样既保证了高低位都能参与索引计算，又减少了运算开销
2. 扩容不再逐个取模：1.7 扩容时每个元素都要重新算一遍 hash % newCapacity，1.8 利用了一个巧妙的特性，元素要么留原位，要么移到 原位置 + 老数组长度，通过一次位运算就能判断，省去了大量取模计算
3. 头插法改尾插法：1.7 插入新节点直接往链表头部塞，扩容时链表会倒过来。多线程环境下，两个线程同时扩容可能把链表搞成环，CPU 直接跑满死循环。1.8 改成尾插法，扩容后顺序不变，避免了成环问题。

![](/images/bagu/8.png)