---
title: skills
date: 2026-04-12 23:16:46
categories:
  - 笔记
  - 大模型与Agent
tags:
  - agent
---
## 什么是skill

通俗来说skill就是一份带目录的说明书，本质来说就是一种**渐进式披露的提示词管理机制**



skill规范将提示词分为三个部分，

1. 元数据（Metadata）**默认加载**        -> 类比 书的**目录**
2. 指令（Instrution）**按需加载**           -> 类比 书的**正文**

3. 资源（Resource）**按需加载**            -> 类比 书的 **附录**

其中最重要的是！！！

只有**元数据**默认被加载到大模型的上下文中，其余两层为按需加载



**可以看出：**与传统Prompt和MCP相比，Agent Skills 最显著的优势在于**大幅降低了 Token 消耗与提示词的复杂度**



## skill的设计使用

1. skill的目录结构：

   每个skill都是单独的一个文件夹，文件夹下有一个全大写的SKILL.md文件，这是skill的元数据+指令

   除此之外还能放其它文件，构成了skill的资源层

   ```
   /skill/skill-name1/
   				|SKILL.md
   				|scripts/xx.py
   				|references/doc.md
   ```

2. skill元数据

   ```
   ---
   name: srt字幕转markdown笔记
   description: 把srt字幕文件转换为markdown笔记
   ---
   ```

   用于描述 Skill 的基本信息，需用 `---` 包裹。它会被优先加载到大模型上下文中，相当于“能力目录”

3. skill指令

   ```
   你是专业的字幕文本处理助手，任务是将sRT 字幕完整转换为 Markdown 笔记，并添加标点、段落格式和必要的截图占位符。要求
   如下:
   1.**内容完整保留**:禁止任何删减、总结或省略，必须逐字保留 SRT 所有文字。
   2.**语言规则**:使用中文书写，保留必要的英文专有名词。
   3.**段落结构**:
       -仅使用一个层级的段落
       -每个段落标题使用## 标题
       -第一段为引言，不使用标题
       -只需要划分2-3个段落
   4. **标点与排版**:
       -为每句话添加合适标点
       -适当划分自然段
   5.**截图占位符规则(按需插入)**
       -当某句符合以下任一条件时，在句末添加截图标记:
       -代码讲解
       -UI 交互操作
       -包含"这么、这里、这儿"等视觉指代词
       -提及网址/链接/地址(包括 GitHub、API endpoint等)
       -任何借助视觉材料更易理解的内容
       -形式必须严格为:
       ```
       Screenshot-[hh:mm:ss]
       ```
       -仅在真正有助理解时插入，不可滥用
       比如:
       ```
       原始SRT
       00:02:08,600-->00:02:09,800
       安装这个工具
       ```
   6. **输出文件格式**:
       - 生成内容保存为单个Markdown 文件，命名为:
       ```
       项目根目录/xxx.md
       ```
   ```

   相当于prompt了，告诉大模型合适调用，以及怎么调用工具





## Skill与MCP的区别

**Skills 与 MCP 是两个目标不同、互为补充的规范**

它们虽然部分功能重叠，但设计的侧重点和使用场景有差异



- Skills 核心在于**提示词的管理和组织**，通过渐进式机制，以轻量，结构化方式拓展模型能力
- **MCP** 的核心在于**工具与服务的标准化接入**， 为模型提供统一，安全的外部工具调用能力



- **Skills 如同“带目录的说明书”** ——逐步展开，按需提供详细操作指引。
- **MCP 如同“标准化工具箱”** ——提供一系列即插即用的工具，模型可直接调用

